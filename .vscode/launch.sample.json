{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Listen for Xdebug",
      "type": "php",
      "request": "launch",
      "port": 9003,
      "hostname": "[ip of the WSL where the docker is]",
      "pathMappings": {
        "/var/www/html": "${workspaceFolder}/backend/backend_api"
      }
    }
  ]
}
