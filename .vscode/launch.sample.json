{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Listen for Xdebug",
      "type": "php",
      "request": "launch",
      "port": 9003,
      "hostname": "[ip of the machine where docker is installed]",
      "pathMappings": {
        "/var/www/html": "${workspaceFolder}/backend/backend_api"
      }
    }
  ]
}
